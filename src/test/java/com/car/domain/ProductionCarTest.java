package com.car.domain;

import static org.junit.jupiter.api.Assertions.*;

class ProductionCarTest {
    // 이름 -> 1~5 자
    // 이동 -> 최초 0, 1 칸씩 이동, 최대 10칸
    // 누락
    // ?? 이미 코드는 만들어져있는데, 테스트 어떻게 하지??
    // 테스트 -> 의존성이 너무 강하면???
}